/**
 * Minimal type declaration for expo-device.
 * expo-device is not yet in package.json; this stub satisfies tsc.
 * Add `expo-device` to dependencies and remove this file once installed.
 */
declare module 'expo-device' {
  /** Total device RAM in bytes, or null if unavailable. */
  export const totalMemory: number | null;
}
