// Placeholder — Agent A's commit replaces this with the real implementation.
// If you see this in main, the merge order was wrong.
export const chatStore = {
  addUserMessage(text: string, _imageUri?: string): string {
    void text;
    return '';
  },
};
